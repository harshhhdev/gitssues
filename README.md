# gitssues

🤔 Confused on what to work on? Gitssues is a cli which fetches a random issue from a GitHub repository

# Building

 1. Clone the repo (`git clone url`)
 2. Install pacakges (`npm i`)
 3. Run the script (`node index.ts [your-username/repository-name]`)
